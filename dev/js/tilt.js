$('.js-tilt').tilt({
    scale: 1.02,
    transition: true,
    // glare: true,
    // maxGlare: 0.25,
    perspective: 600,
    speed: 1000,
    maxTilt: 20,
    easing: "cubic-bezier(.03,.98,.52,.99)"
});

$('.js-tilt-index-hero').tilt({
    scale: 1.02,
    transition: true,
    // glare: true,
    // maxGlare: 0.25,
    perspective: 10000,
    speed: 1000,
    maxTilt: 10,
    easing: "cubic-bezier(.03,.98,.52,.99)"
});

$('.js-tilt-index-medium').tilt({
    scale: 1.04,
    transition: true,
    perspective: 600,
    speed: 1000,
    maxTilt: 20,
    easing: "cubic-bezier(.03,.98,.52,.99)"
});